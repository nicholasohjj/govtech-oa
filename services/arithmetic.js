const addOperation = async (number1, number2) => {
    // backend
}

const subtractOperation = async (number1, number2) => {
    // backend
}

export default {
    addOperation,
    subtractOperation
}

